<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.stuart.stuartapp">

    <uses-permission android:name="android.permission.VIBRATE"/>

    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-permission android:name="android.permission.READ_CONTACTS"/>

    <uses-permission android:name="android.permission.WRITE_CONTACTS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.TwoColorBallActivity"></activity>

        <activity android:name=".activity.SSQHistoryActivity"></activity>

        <activity android:name=".activity.DemoActivity">
            <intent-filter>
                <action android:name="demo"></action>
                <category android:name="android.intent.category.DEFAULT"></category>
            </intent-filter>
        </activity>

        <activity android:name=".demo.activity.AddContacts">

        </activity>

        <activity android:name=".demo.activity.TestRecycler">
            <intent-filter>
                <action android:name="testRecycler"></action>
                <category android:name="android.intent.category.DEFAULT"></category>
            </intent-filter>
        </activity>

        <activity android:name=".demo.activity.SampleActivity"></activity>
        <provider
            android:authorities="com.stuart.stuartapp"
            android:name=".db.StuartProvider"/>

    </application>

</manifest>